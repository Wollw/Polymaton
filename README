Opens the file default.json in the "Polymaton" directory on the sdcard at launch.
Examples boards can be found in the "Boards" directory.
Birth rules, survival rules, and initial board state can be set in the board files.
All boards are in json format.
